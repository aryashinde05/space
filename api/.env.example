# Flask server
PORT=4000
CORS_ORIGIN=http://localhost:5500

# PostgreSQL (AWS RDS or local)
PGHOST=localhost
PGPORT=5432
PGDATABASE=space
PGUSER=postgres
PGPASSWORD=postgres
# For RDS with SSL
PGSSL=disable
