# PostgreSQL connection
PGHOST=localhost
PGPORT=5432
PGDATABASE=space
PGUSER=postgres
PGPASSWORD=postgres
# CORS allowed origin for local dev
CORS_ORIGIN=http://localhost:5500
# Server port
PORT=4000
